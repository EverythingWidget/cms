tinymce.PluginManager.add('grid', function (editor, url) {
   // Add a button that opens a window


   editor.addButton('grid', {
      text: "Row",
      tooltip: 'Row',
      onclick: function () {
         var b =editor.selection.getNode()
         editor.insertContent("<div class='row'><div class='col-xs-6'>sdfdsf</div></div>");
      }
   });
   /*editor.addMenuItem('example', {
    text: 'EW Media',
    context: 'tools',
    onclick: function () {
    // Open window with a specific url
    editor.windowManager.open({
    title: 'TinyMCE site',
    url: 'http://www.tinymce.com',
    width: 800,
    height: 600,
    buttons: [{
    text: 'Close',
    onclick: 'close'
    }]
    });
    }
    });*/

});